server:
  port: 9002
spring:
  application:
    name: payment
  h2:
    console:
      enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9003/eureka
    fetch-registry: true
    register-with-eureka: true
  instance:
    hostname: localhost